import { CompaniesChargingStationsInterceptor } from './companies-charging-stations.interceptor';

describe('CompaniesChargingStationsInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new CompaniesChargingStationsInterceptor()).toBeDefined();
  });
});
