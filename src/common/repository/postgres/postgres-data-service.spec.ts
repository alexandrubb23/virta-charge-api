import { PostgresDataService } from './postgres-data-service';

describe('PostgresDataService', () => {
  it('should be defined', () => {
    expect(new PostgresDataService()).toBeDefined();
  });
});
