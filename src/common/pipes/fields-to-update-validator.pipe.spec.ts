import { FieldsToUpdateValidatorPipe } from './fields-to-update-validator.pipe';

describe('FieldsToUpdateValidatorPipePipe', () => {
  it('should be defined', () => {
    expect(new FieldsToUpdateValidatorPipe()).toBeDefined();
  });
});
